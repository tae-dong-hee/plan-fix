spring:
  jpa:
    hibernate:
      # Test contexts share one disposable PostgreSQL container per test JVM.
      ddl-auto: update

tour-api:
  base-url: https://apis.data.go.kr/B551011/KorService2
  service-key: ""
  mobile-os: ETC
  mobile-app: planfix
  page-size: 100
  call-interval-ms: 200
  connect-timeout-ms: 15000
  read-timeout-ms: 60000

logging:
  level:
    org.hibernate.SQL: warn
